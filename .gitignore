# Java / Spring Boot
target/
*.class
*.jar
*.war
.mvn/wrapper/maven-wrapper.jar
HELP.md

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
# application-local.yml is now the committed default (local) profile - non-secret sandbox config
# only. Secrets stay in the still-ignored credentials profile (or env vars).
application-credentials.yml

# Logs
*.log
